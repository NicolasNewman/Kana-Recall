import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'com.nicolasnewman.kanarecall',
	appName: 'Kana-Recall',
	webDir: '../../frontend/build'
};

export default config;
